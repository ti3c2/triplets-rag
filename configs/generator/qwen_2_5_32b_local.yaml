# @package generator
# Externally-managed OpenAI-compatible vLLM server. URL comes from
# VLLM_BASE_URL in .env unless the caller serves this model through a router.
kind: vllm
model_name: Qwen/Qwen2.5-32B-Instruct
base_url: null
temperature: 0.0
max_tokens: 1024
top_p: 1.0
