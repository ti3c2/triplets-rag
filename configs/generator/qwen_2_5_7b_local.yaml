# @package generator
# Use an externally-managed vLLM server (e.g. one you already started on
# localhost:7113). Server URL comes from VLLM_BASE_URL in .env.
kind: vllm
model_name: Qwen/Qwen2.5-7B-Instruct
temperature: 0.0
max_tokens: 1024
top_p: 1.0
