# @package _global_
defaults:
  - /dataset: squad_dev_1k
  - /chunking: sliding_512_64
  - /generator: gpt4o
  - /embedder: bge_small
  - /indexer: faiss_flat_triplets
  - /retriever: triplet_top_2
  - /student: qwen_2_5_1_5b
  - /inference_strategy: triplet
  - /metrics: lexical_only
  - _self_

experiment_name: squad_capacity_gap_triplet
seed: 42

budget:
  total_context_items: 10
  num_triplets: 2
  per_triplet_contexts: 5
  matching_mode: item

preprocessing:
  num_questions_per_chunk: 5
  question_prompt: complex
  answer_prompt: rag_default
  max_questions_total: null
  dedupe_questions: true
  dedupe_threshold: 0.95

filtering:
  enabled: false
  faithfulness_threshold: 0.7
  judge_model: null

notes: "RQ3 capacity gap — teacher gpt-4o, student Qwen-2.5-1.5B; triplet RAG. Pair with squad_capacity_gap_vanilla; preprocessing artifacts are shared."
